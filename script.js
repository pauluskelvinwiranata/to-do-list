const taskInput=document.getElementById("taskInput"),taskList=document.getElementById("taskList"),addButton=document.getElementById("addButton"),tasks=JSON.parse(localStorage.getItem("tasks"))||[];function saveTasks(){localStorage.setItem("tasks",JSON.stringify(tasks))}function addTask(t){tasks.push({text:t,completed:!1}),saveTasks(),renderTasks(),taskInput.value=""}function toggleComplete(t){tasks[t].completed=!tasks[t].completed,saveTasks(),renderTasks()}function deleteTask(t){tasks.splice(t,1),saveTasks(),renderTasks()}function renderTasks(){taskList.innerHTML="",tasks.forEach((t,e)=>{let s=document.createElement("li");s.classList.add("task-item");let a=document.createElement("input");a.type="checkbox",a.checked=t.completed,a.addEventListener("change",()=>toggleComplete(e));let n=document.createElement("span");n.classList.add("task-text"),n.textContent=t.text;let d=document.createElement("span");d.classList.add("delete-button"),d.textContent="Hapus",d.addEventListener("click",()=>deleteTask(e)),s.appendChild(a),s.appendChild(n),s.appendChild(d),taskList.appendChild(s)})}taskInput.addEventListener("keyup",t=>{"Enter"===t.key&&""!==taskInput.value.trim()&&addTask(taskInput.value.trim())}),addButton.addEventListener("click",()=>{""!==taskInput.value.trim()&&addTask(taskInput.value.trim())}),renderTasks();